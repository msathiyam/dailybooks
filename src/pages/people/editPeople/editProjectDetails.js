import React from "react";

const EditProjectDetails = () => {
  return (
          
      <div >
       
        <div className="mt-4 w-full">
            <div className="mt-7">
            <div className="flex"><h3 className=" text-[18px] font-semibold">UX Designer-ONEUX Creative Studio(2020-present)</h3>
            <i class="ri-pencil-line font-extrabold text-blue-700 ml-2"></i></div>
            <p className="text-base text-gray-400 ">
            We have to set initial state value inside constructor function and set
             click event handler of the element upon which click, results in changing state.
              Then pass the function to the click handler and change the state of the component inside the function using setState. The setState function used to change the state of the component directly or with the callback approach as mentioned below.
            </p>
            </div>
            <div className="mt-10">
           <div className="flex"> <h3 className="text-[18px] font-semibold">UX Designer-Lollypop Design(2018-2020)</h3>
           <i class="ri-pencil-line font-extrabold text-blue-700 ml-2"></i>
           </div>
            <p className="text-base text-gray-400 ">
            We have to set initial state value inside constructor function and set
             click event handler of the element upon which click, results in changing state.
              Then pass the function to the click handler and change the state of the component inside the function using setState. The setState function used to change the state of the component directly or with the callback approach as mentioned below.
            </p>
            </div>
            <div className="mt-10">
           <div className="flex"> <h3 className="text-[18px] font-semibold">UI/UX Dope Design Studio</h3>
           <i class="ri-pencil-line font-extrabold text-blue-700 ml-2"></i></div>
            <p className="text-base text-gray-400">
            We have to set initial state value inside constructor function and set
             click event handler of the element upon which click, results in changing state.
              Then pass the function to the click handler and change the state of the component inside the function using setState. The setState function used to change the state of the component directly or with the callback approach as mentioned below.
            </p>
            </div>
           
        </div>
       
        
      </div>
 
  );
};

export default EditProjectDetails;
